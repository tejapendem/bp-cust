<mvc:View controllerName="bp.cust.ui.controller.Wizard"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:form="sap.ui.layout.form"
    xmlns:layout="sap.ui.layout"
    xmlns:core="sap.ui.core"
    xmlns:mc="sap.suite.ui.microchart"
    height="100%">

    <Page id="wizardPage"
          showHeader="false"
          backgroundDesign="List"
          class="createBpPage">
        <content>
            <VBox class="cbpOuterPadding">

                <!-- ── Page Header ──────────────────────────────────── -->
                <HBox alignItems="Center" justifyContent="SpaceBetween" class="cbpPageHeader">
                    <HBox alignItems="Center">
                        <FlexBox justifyContent="Center" alignItems="Center" class="cbpHeaderIcon">
                            <core:Icon src="sap-icon://create" size="1.6rem" color="#0a6ed1" />
                        </FlexBox>
                        <VBox class="sapUiSmallMarginBegin">
                            <Title text="{= ${wizardData>/isReadOnly} ? 'Business Partner Details' : 'Create Business Partner' }" level="H2" class="cbpPageTitle" />
                            <Text text="{= ${wizardData>/isReadOnly} ? 'View business partner master data' : 'Complete each step to register a new business partner' }" class="cbpPageSubtitle" />
                        </VBox>
                    </HBox>
                    <HBox>
                        <Button icon="sap-icon://nav-back" text="Back" type="Transparent" press="onNavBack" class="cbpBackBtn sapUiSmallMarginEnd" />
                        <Button text="Save as Draft" icon="sap-icon://save" press="onSaveDraft" visible="{= !${wizardData>/isReadOnly} }" class="cbpDraftBtn" />
                    </HBox>
                </HBox>

                <!-- ── Progress Card ────────────────────────────────── -->
                <VBox class="cbpProgressCard sapUiSmallMarginTop">
                    <HBox alignItems="Center" class="cbpProgressInner">
                        <mc:RadialMicroChart
                            id="progressChart"
                            size="M"
                            percentage="{wizardData>/progress}"
                            valueColor="{wizardData>/progressColor}" />
                        <VBox class="sapUiMediumMarginBegin">
                            <Title text="Completion Status" level="H4" class="cbpProgressTitle" />
                            <Text text="{wizardData>/progressText}" class="cbpProgressText" />
                        </VBox>
                    </HBox>
                </VBox>

                <!-- ── Wizard Card ──────────────────────────────────── -->
                <VBox class="cbpWizardCard sapUiSmallMarginTop sapUiMediumMarginBottom">
                    <Wizard id="bpWizard"
                            complete="onWizardCompleted"
                            stepActivate="onStepActivate"
                            showNextButton="false"
                            renderMode="Page"
                            class="cbpWizard">

                    <WizardStep id="step1" title="Basic Info (000000)" icon="sap-icon://account" validated="true">
                        <form:SimpleForm editable="true" layout="ResponsiveGridLayout" labelSpanXL="3" labelSpanL="3" labelSpanM="3" labelSpanS="12" columnsXL="2" columnsL="2" columnsM="1">
                            <core:Title text="1. Basic Info"/>
                            
                            <Label text="Business Partner" required="true"/>
                            <Select id="categorySelect" selectedKey="{wizardData>/BusinessPartnerCategory}" items="{path: '/VH_BusinessPartnerCategory', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                <core:Item key="{code}" text="{name}"/>
                            </Select>

                            <Label text="BP Type" required="true"/>
                            <Select id="bpTypeSelect" selectedKey="{wizardData>/BPType}" items="{path: '/VH_BPType', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                <core:Item key="{code}" text="{name}"/>
                            </Select>
                            
                            <Label text="Grouping" required="true"/>
                            <Select id="groupingSelect" selectedKey="{wizardData>/Grouping}" items="{path: '/VH_Grouping', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }" change="onGroupingChange">
                                <core:Item key="{code}" text="{code} - {name}"/>
                            </Select>
                        </form:SimpleForm>
                        <HBox justifyContent="End" class="sapUiMediumMarginTop">
                            <Button text="Step 2" type="Emphasized" press="onNextStep" />
                        </HBox>
                    </WizardStep>

                    <WizardStep id="step2" title="{= ${wizardData>/BusinessPartnerCategory} === '1' ? 'Address Details (Name)' : 'Address Details (Identity,Standard Address,Communication)' }" icon="sap-icon://map-2" validated="true">
                        <!-- Form for Organization -->
                        <HBox width="100%" wrap="Wrap" visible="{= ${wizardData>/BusinessPartnerCategory} !== '1' }">
                            
                            <!-- Left Column: Identity & Communication -->
                            <VBox width="48%" class="sapUiSmallMarginEnd">
                                <form:SimpleForm editable="true" layout="ResponsiveGridLayout" labelSpanXL="4" labelSpanL="4" labelSpanM="4" labelSpanS="12">
                                    <core:Title text="Identity"/>
                                    
                                    <Label text="Title" required="true"/>
                                    <Select selectedKey="{wizardData>/Title}" items="{path: '/VH_Title', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                        <core:Item key="{code}" text="{code} - {name}"/>
                                    </Select>

                                    <Label text="Name" required="true"/>
                                     <Input id="nameInput" 
                                            value="{wizardData>/Name}" 
                                            placeholder="Full / Company Name" 
                                            editable="{= !${wizardData>/isReadOnly} }"
                                            showSuggestion="true"
                                            suggest="onNameSuggest"
                                            liveChange="onNameInputLiveChange"
                                            suggestionItems="{suggestions>/}">
                                        <suggestionItems>
                                            <core:Item key="{suggestions>CustomerID}" text="{= ${suggestions>City} ? ${suggestions>Name} + ' (' + ${suggestions>City} + ')' : ${suggestions>Name} }"/>
                                        </suggestionItems>
                                    </Input>
                                    
                                    <Label text="Search terms 1/2" required="true"/>
                                    <Input value="{wizardData>/SearchTerm1}" placeholder="Term 1" editable="{= !${wizardData>/isReadOnly} }">
                                        <layoutData><layout:GridData span="XL4 L4 M4 S6" /></layoutData>
                                    </Input>
                                    <Input value="{wizardData>/SearchTerm2}" placeholder="Term 2" editable="{= !${wizardData>/isReadOnly} }">
                                        <layoutData><layout:GridData span="XL4 L4 M4 S6" /></layoutData>
                                    </Input>
                                    
                                    <core:Title text="Communication"/>

                                    <Label text="Language"/>
                                    <Select selectedKey="{wizardData>/Language}" items="{path: '/VH_Language', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                        <core:Item key="{code}" text="{code} - {name}"/>
                                    </Select>

                                    <Label text="Mobile Number" required="true"/>
                                    <Select id="mobileCodeSelect" selectedKey="{wizardData>/MobileCountryCode}" items="{path: '/VH_MobileCountryCode', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                        <layoutData><layout:GridData span="XL3 L3 M3 S4" /></layoutData>
                                        <core:Item key="{code}" text="{code}"/>
                                    </Select>
                                    <Input id="mobileInput" value="{wizardData>/MobileNumber}" placeholder="Digits only" editable="{= !${wizardData>/isReadOnly} }">
                                        <layoutData><layout:GridData span="XL5 L5 M5 S8" /></layoutData>
                                    </Input>
                                    
                                    <Label text="Telephone"/>
                                    <Input value="{wizardData>/Telephone}" placeholder="Landline, Office, etc." editable="{= !${wizardData>/isReadOnly} }"/>

                                    <Label text="Email" required="true"/>
                                    <Input id="emailInput" value="{wizardData>/Email}" type="Email" placeholder="user@example.com" editable="{= !${wizardData>/isReadOnly} }"/>
                                </form:SimpleForm>
                            </VBox>

                            <!-- Right Column: Location Details -->
                            <VBox width="48%">
                                <form:SimpleForm editable="true" layout="ResponsiveGridLayout" labelSpanXL="4" labelSpanL="4" labelSpanM="4" labelSpanS="12">
                                    <Toolbar>
                                        <Title text="Standard Address" level="H5"/>
                                        <ToolbarSpacer/>
                                        <Button icon="{= ${wizardData>/showExtendedAddress} ? 'sap-icon://less' : 'sap-icon://add' }" type="Transparent" press="onToggleExtendedAddress" tooltip="Show/Hide extended address fields" />
                                    </Toolbar>

                                    <Label text="Street/House Number"/>
                                    <Input value="{wizardData>/StreetAddress}" placeholder="Street" editable="{= !${wizardData>/isReadOnly} }">
                                        <layoutData><layout:GridData span="XL6 L6 M6 S8" /></layoutData>
                                    </Input>
                                    <Input value="{wizardData>/HouseNumber}" placeholder="House No." editable="{= !${wizardData>/isReadOnly} }">
                                        <layoutData><layout:GridData span="XL2 L2 M2 S4" /></layoutData>
                                    </Input>

                                    <Label text="Postal Code/City"/>
                                    <Input value="{wizardData>/PostalCode}" placeholder="Postal Code" editable="{= !${wizardData>/isReadOnly} }">
                                        <layoutData><layout:GridData span="XL3 L3 M3 S4" /></layoutData>
                                    </Input>
                                    <Input value="{wizardData>/City}" placeholder="City" editable="{= !${wizardData>/isReadOnly} }">
                                        <layoutData><layout:GridData span="XL5 L5 M5 S8" /></layoutData>
                                    </Input>

                                    <Label text="Country/Reg."/>
                                    <Select id="countrySelect" selectedKey="{wizardData>/Country}" items="{path: '/VH_Country', parameters: {$filter: 'isActive eq true'}}" change="onCountryChange" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                        <layoutData><layout:GridData span="XL3 L3 M3 S4" /></layoutData>
                                        <core:Item key="{code}" text="{code} - {name}"/>
                                    </Select>
                                    <Label text="Region" visible="{= !!${wizardData>/Country} }">
                                        <layoutData><layout:GridData span="XL2 L2 M2 S3" /></layoutData>
                                    </Label>
                                    <Select id="regionSelect" selectedKey="{wizardData>/Region}" items="{path: '/VH_Region', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }" visible="{= !!${wizardData>/Country} }">
                                        <layoutData><layout:GridData span="XL3 L3 M3 S5" /></layoutData>
                                        <core:Item key="{code}" text="{name}"/>
                                    </Select>

                                    <!-- Extended Address Fields -->
                                    <Label text="Building Code" visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/BuildingCode}" placeholder="Building Code" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}">
                                        <layoutData><layout:GridData span="XL3 L3 M3 S4" /></layoutData>
                                    </Input>
                                    <Label text="Room" visible="{wizardData>/showExtendedAddress}">
                                        <layoutData><layout:GridData span="XL1 L1 M1 S2" /></layoutData>
                                    </Label>
                                    <Input value="{wizardData>/Room}" placeholder="Room" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}">
                                        <layoutData><layout:GridData span="XL1 L1 M1 S2" /></layoutData>
                                    </Input>
                                    <Label text="Floor" visible="{wizardData>/showExtendedAddress}">
                                        <layoutData><layout:GridData span="XL1 L1 M1 S2" /></layoutData>
                                    </Label>
                                    <Input value="{wizardData>/Floor}" placeholder="Floor" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}">
                                        <layoutData><layout:GridData span="XL2 L2 M2 S2" /></layoutData>
                                    </Input>

                                    <Label text="c/o" visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/CareOf}" placeholder="c/o" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}"/>

                                    <Label text="Street 2" visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/Street2}" placeholder="Street 2" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}"/>

                                    <Label text="Street 3" visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/Street3}" placeholder="Street 3" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}"/>

                                    <Label text="Suppl." visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/StreetSupplement}" placeholder="Street Supplement" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}"/>

                                    <Label text="Street 4" visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/Street4}" placeholder="Street 4" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}"/>

                                    <Label text="Street 5" visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/Street5}" placeholder="Street 5" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}"/>

                                    <Label text="District" visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/District}" placeholder="District" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}"/>

                                    <Label text="Different City" visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/DifferentCity}" placeholder="Different City" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}"/>

                                    <Label text="Time Zone" visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/TimeZone}" placeholder="Time Zone" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}">
                                        <layoutData><layout:GridData span="XL3 L3 M3 S4" /></layoutData>
                                    </Input>
                                    <Label text="Tax Juris." visible="{wizardData>/showExtendedAddress}">
                                        <layoutData><layout:GridData span="XL2 L2 M2 S3" /></layoutData>
                                    </Label>
                                    <Input value="{wizardData>/TaxJurisdiction}" placeholder="Tax Jurisdiction" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}">
                                        <layoutData><layout:GridData span="XL3 L3 M3 S5" /></layoutData>
                                    </Input>

                                    <Label text="Transportation Zone" visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/TransportationZone}" placeholder="Transportation Zone" width="40%" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}"/>

                                    <Label text="Structure Group" visible="{wizardData>/showExtendedAddress}"/>
                                    <Input value="{wizardData>/StructureGroup}" placeholder="Structure Group" width="40%" editable="{= !${wizardData>/isReadOnly} }" visible="{wizardData>/showExtendedAddress}"/>
                                </form:SimpleForm>
                            </VBox>
                        </HBox>

                        <!-- Form for Person -->
                        <form:SimpleForm editable="true" layout="ResponsiveGridLayout" labelSpanXL="3" labelSpanL="3" labelSpanM="3" labelSpanS="12" columnsXL="2" columnsL="2" columnsM="1" visible="{= ${wizardData>/BusinessPartnerCategory} === '1' }">
                            <core:Title text="Name"/>

                            <Label text="Firstname" required="true"/>
                            <Input value="{wizardData>/FirstName}" placeholder="First Name" editable="{= !${wizardData>/isReadOnly} }"/>

                            <Label text="Lastname" required="true"/>
                            <Input value="{wizardData>/LastName}" placeholder="Last Name" editable="{= !${wizardData>/isReadOnly} }"/>
                            
                            <Label text="Search terms 1/2"/>
                            <HBox>
                                <Input value="{wizardData>/SearchTerm1}" placeholder="Term 1" editable="{= !${wizardData>/isReadOnly} }"/>
                                <Input value="{wizardData>/SearchTerm2}" placeholder="Term 2" class="sapUiSmallMarginBegin" editable="{= !${wizardData>/isReadOnly} }"/>
                            </HBox>

                            <core:Title text="Communication"/>

                            <Label text="Language"/>
                            <Select selectedKey="{wizardData>/Language}" items="{path: '/VH_Language', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                <core:Item key="{code}" text="{code} - {name}"/>
                            </Select>

                            <Label text="Country"/>
                            <Select selectedKey="{wizardData>/Country}" items="{path: '/VH_Country', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                <core:Item key="{code}" text="{code} - {name}"/>
                            </Select>
                        </form:SimpleForm>
                        <HBox justifyContent="SpaceBetween" class="sapUiMediumMarginTop">
                            <Button text="Previous" type="Transparent" press="onPrevStep" />
                            <Button text="Step 3" type="Emphasized" press="onNextStep" />
                        </HBox>
                    </WizardStep>

                    <WizardStep id="step3" title="Identification" icon="sap-icon://loan" validated="true">
                        <form:SimpleForm editable="true" layout="ResponsiveGridLayout" labelSpanXL="3" labelSpanL="3" labelSpanM="3" labelSpanS="12" columnsXL="2" columnsL="2" columnsM="1">
                            <core:Title text="3. Tax Information"/>
                            
                            <Label text="Tax Category" required="true"/>
                            <Select id="taxCategorySelect" selectedKey="{wizardData>/TaxCategory}" items="{path: '/VH_TaxCategory', parameters: {$filter: 'isActive eq true'}}" forceSelection="true" editable="{= !${wizardData>/isReadOnly} }" change="onTaxCategoryChange">
                                <core:Item key="{code}" text="{code} - {name}"/>
                            </Select>
                            
                            <Label text="Tin Number" required="{= ${wizardData>/TaxCategory}.indexOf('UG') === 0 }"/>
                            <HBox alignItems="Center">
                                <Input id="taxNumberInput" value="{wizardData>/TaxNumber}" placeholder="TIN / Tax ID Number" editable="{= !${wizardData>/isReadOnly} }" width="250px" liveChange="onTinNumberLiveChange"/>
                                <Button id="validateVatBtn" text="Validate" icon="sap-icon://accept" type="Ghost" press="onValidateTaxNumber" class="sapUiSmallMarginBegin" visible="{= ${wizardData>/TaxCategory}.indexOf('UG') === 0 &amp;&amp; !${wizardData>/isReadOnly} }" enabled="{= !!${wizardData>/TaxNumber} }"/>
                                <core:Icon src="sap-icon://message-success" color="#2b7c2b" class="sapUiSmallMarginBegin" visible="{wizardData>/vatValidationSuccess}" />
                                <core:Icon src="sap-icon://message-error" color="#bb0000" class="sapUiSmallMarginBegin" visible="{wizardData>/vatValidationFailed}" />
                                <Text text="{wizardData>/vatValidationMessage}" class="sapUiSmallMarginBegin" />
                            </HBox>

                            <Label text="Tax Number"/>
                            <HBox alignItems="Center">
                                <Input id="taxNumberDupInput" value="{wizardData>/TaxNumberDup}" placeholder="Auto-filled from Tin Number" editable="false" width="250px"/>
                                <Button id="validateTaxDupBtn" text="Validate" icon="sap-icon://accept" type="Ghost" press="onValidateTaxNumberDup" class="sapUiSmallMarginBegin" visible="{= ${wizardData>/TaxCategory}.indexOf('UG') === 0 &amp;&amp; !${wizardData>/isReadOnly} }" enabled="{= !!${wizardData>/TaxNumberDup} }"/>
                                <core:Icon src="sap-icon://message-success" color="#2b7c2b" class="sapUiSmallMarginBegin" visible="{wizardData>/taxDupValidationSuccess}" />
                                <core:Icon src="sap-icon://message-error" color="#bb0000" class="sapUiSmallMarginBegin" visible="{wizardData>/taxDupValidationFailed}" />
                                <Text text="{wizardData>/taxDupValidationMessage}" class="sapUiSmallMarginBegin" />
                            </HBox>

                            <Label text="Tax Status"/>
                            <Select selectedKey="{wizardData>/TaxStatus}" items="{path: '/VH_TaxStatus', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                <core:Item key="{code}" text="{name}"/>
                            </Select>
                        </form:SimpleForm>
                        <HBox justifyContent="SpaceBetween" class="sapUiMediumMarginTop">
                            <Button text="Previous" type="Transparent" press="onPrevStep" />
                            <Button text="Step 4" type="Emphasized" press="onNextStep" />
                        </HBox>
                    </WizardStep>

                    <WizardStep id="step4" title="Company Details (FLCU00)" icon="sap-icon://factory" validated="true">
                        <VBox>
                            <Table items="{wizardData>/CompanyCodes}" noDataText="No Company Codes defined. Click '+' to add.">
                                <headerToolbar>
                                    <OverflowToolbar>
                                        <Title text="Company Code Assignments"/>
                                        <ToolbarSpacer />
                                        <Button icon="sap-icon://add" text="Add Company Code" press="onAddCompanyCode" type="Ghost" visible="{= !${wizardData>/isReadOnly} }"/>
                                    </OverflowToolbar>
                                </headerToolbar>
                                <columns>
                                    <Column><Text text="Company Code"/></Column>
                                    <Column><Text text="Is BP"/></Column>
                                    <Column><Text text="Is Customer"/></Column>
                                    <Column hAlign="End" width="50px" visible="{= !${wizardData>/isReadOnly} }"></Column>
                                </columns>
                                <items>
                                    <ColumnListItem>
                                        <cells>
                                            <Select selectedKey="{wizardData>CompanyCode}" items="{path: '/VH_CompanyCode', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }" change="onCompanyCodeChange">
                                                <core:Item key="{code}" text="{code} - {name}"/>
                                            </Select>
                                            <CheckBox selected="{wizardData>IsBP}" editable="{= !${wizardData>/isReadOnly} }"/>
                                            <CheckBox selected="{wizardData>IsCustomer}" editable="{= !${wizardData>/isReadOnly} }"/>
                                            <Button icon="sap-icon://delete" type="Reject" press="onRemoveCompanyCode" visible="{= !${wizardData>/isReadOnly} }"/>
                                        </cells>
                                    </ColumnListItem>
                                </items>
                            </Table>
                            
                            <IconTabBar items="{wizardData>/CompanyCodes}" expandable="false" applyContentPadding="false" class="sapUiMediumMarginTop">
                                <items>
                                    <IconTabFilter icon="sap-icon://factory" text="CC: {wizardData>CompanyCode}" design="Horizontal">
                                        <form:SimpleForm editable="true" layout="ResponsiveGridLayout" labelSpanXL="3" labelSpanL="3" labelSpanM="3" labelSpanS="12" columnsXL="2" columnsL="2" columnsM="1">
                                            <core:Title text="Account Management"/>
                                            <Label text="Reconciliation Account"/>
                                            <Select selectedKey="{wizardData>ReconciliationAccount}" items="{path: '/VH_ReconciliationAccount', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                                <core:Item key="{code}" text="{code} - {name}"/>
                                            </Select>
                                        </form:SimpleForm>
                                    </IconTabFilter>
                                </items>
                            </IconTabBar>
                        </VBox>
                        <HBox justifyContent="SpaceBetween" class="sapUiMediumMarginTop">
                            <Button text="Previous" type="Transparent" press="onPrevStep" />
                            <HBox>
                                <Button text="Step 5" type="Emphasized" press="onNextStep" />
                            </HBox>
                        </HBox>
                    </WizardStep>

                    <WizardStep id="step5" title="Sales Area (FLCU01)" icon="sap-icon://sales-order" validated="true">
                        <VBox>
                            <Table items="{wizardData>/SalesAreas}" noDataText="No Sales Areas defined. Click '+' to add.">
                                <headerToolbar>
                                    <OverflowToolbar>
                                        <Title text="Sales Area Assignments"/>
                                        <ToolbarSpacer />
                                        <Button icon="sap-icon://add" text="Add Sales Area" press="onAddSalesArea" type="Ghost" visible="{= !${wizardData>/isReadOnly} }"/>
                                    </OverflowToolbar>
                                </headerToolbar>
                                <columns>
                                    <Column><Text text="Sales Organization"/></Column>
                                    <Column><Text text="Distribution Channel"/></Column>
                                    <Column><Text text="Division"/></Column>
                                    <Column hAlign="End" width="50px" visible="{= !${wizardData>/isReadOnly} }"></Column>
                                </columns>
                                <items>
                                    <ColumnListItem>
                                        <cells>
                                            <Select selectedKey="{wizardData>SalesOrganization}" items="{path: '/VH_SalesOrganization', parameters: {$filter: 'isActive eq true'}}" change="onSalesOrgChange" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                                <core:Item key="{code}" text="{code} - {name}"/>
                                            </Select>
                                            <Select selectedKey="{wizardData>DistributionChannel}" items="{path: '/VH_DistributionChannel', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                                <core:Item key="{code}" text="{code} - {name}"/>
                                            </Select>
                                            <Select selectedKey="{wizardData>Division}" items="{path: '/VH_Division', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                                <core:Item key="{code}" text="{code} - {name}"/>
                                            </Select>
                                            <Button icon="sap-icon://delete" type="Reject" press="onRemoveSalesArea" visible="{= !${wizardData>/isReadOnly} }"/>
                                        </cells>
                                    </ColumnListItem>
                                </items>
                            </Table>
                        </VBox>
                        <HBox justifyContent="SpaceBetween" class="sapUiMediumMarginTop">
                            <Button text="Previous" type="Transparent" press="onPrevStep" />
                            <HBox>
                                <Button text="Skip" icon="sap-icon://journey-depart" type="Ghost" press="onSkipStep" class="sapUiSmallMarginEnd" visible="{= !${wizardData>/isReadOnly} }" />
                                <Button text="Step 6" type="Emphasized" press="onNextStep" />
                            </HBox>
                        </HBox>
                    </WizardStep>

                    <WizardStep id="step6" title="Customer Info" icon="sap-icon://money-bills" validated="true">
                        <IconTabBar items="{wizardData>/SalesAreas}" expandable="false" applyContentPadding="false">
                            <items>
                                <IconTabFilter icon="sap-icon://org-chart" text="Sales Org: {wizardData>SalesOrganization}" design="Horizontal">
                                    <form:SimpleForm editable="true" layout="ResponsiveGridLayout" labelSpanXL="3" labelSpanL="3" labelSpanM="3" labelSpanS="12" columnsXL="2" columnsL="2" columnsM="1">
                                        <core:Title text="Sales Logic"/>
                                        
                                        <Label text="Customer Group"/>
                                        <Select selectedKey="{wizardData>CustomerGroup}" items="{path: '/VH_CustomerGroup', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{code} - {name}"/>
                                        </Select>
                                        
                                        <Label text="Currency"/>
                                        <Select selectedKey="{wizardData>Currency}" items="{path: '/VH_Currency', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{name}"/>
                                        </Select>
                                        
                                        <Label text="Exchange Rate Type"/>
                                        <Select selectedKey="{wizardData>ExchangeRateType}" items="{path: '/VH_ExchangeRateType', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{name}"/>
                                        </Select>
                                        
                                        <Label text="Pricing Procedure"/>
                                        <Select selectedKey="{wizardData>CustomerPricingProcedure}" items="{path: '/VH_PricingProcedure', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{name}"/>
                                        </Select>
                                        
                                        <Label text="Statistics Group"/>
                                        <Select selectedKey="{wizardData>CustomerStatsGroup}" items="{path: '/VH_StatisticsGroup', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{name}"/>
                                        </Select>
 
                                        <core:Title text="Billing &amp; Tax"/>
 
                                        <Label text="Payment Terms"/>
                                        <Select selectedKey="{wizardData>PaymentTerms}" items="{path: '/VH_PaymentTerms', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{name}"/>
                                        </Select>
 
                                        <Label text="Incoterms"/>
                                        <Select selectedKey="{wizardData>Incoterms}" items="{path: '/VH_Incoterms', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{name}"/>
                                        </Select>
 
                                        <Label text="Account Assignment Group"/>
                                        <Select selectedKey="{wizardData>AccountAssignmentGroup}" items="{path: '/VH_AccountAssignmentGroup', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{code} - {name}"/>
                                        </Select>
 
                                        <Label text="Tax Classification"/>
                                        <Select selectedKey="{wizardData>TaxClassification}" items="{path: '/VH_TaxClassification', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{code} - {name}"/>
                                        </Select>
 
                                        <Label text="Output Tax Country"/>
                                        <Select selectedKey="{wizardData>OutputTaxCountry}" items="{path: '/VH_Country', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{code} - {name}"/>
                                        </Select>
 
                                        <Label text="Output Tax Category"/>
                                        <Select selectedKey="{wizardData>OutputTaxCategory}" items="{path: '/VH_OutputTaxCategory', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{name}"/>
                                        </Select>
 
                                        <Label text="Customer Data 2"/>
                                        <Select selectedKey="{wizardData>CustomerData2}" items="{path: '/VH_CustomerData2', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" editable="{= !${wizardData>/isReadOnly} }">
                                            <core:Item key="{code}" text="{code} - {name}"/>
                                        </Select>
                                    </form:SimpleForm>
                                </IconTabFilter>
                            </items>
                        </IconTabBar>
                        <HBox justifyContent="SpaceBetween" class="sapUiMediumMarginTop">
                            <Button text="Previous" type="Transparent" press="onPrevStep" />
                            <HBox>
                                <Button text="Skip" icon="sap-icon://journey-depart" type="Ghost" press="onSkipStep" class="sapUiSmallMarginEnd" visible="{= !${wizardData>/isReadOnly} }" />
                                <Button text="Step 7" type="Emphasized" press="onNextStep" />
                            </HBox>
                        </HBox>
                    </WizardStep>
 
                    <WizardStep id="step7" title="UKM000 SAP Credit Management" icon="sap-icon://collections-insight" validated="true">
                        <VBox>
                            <form:SimpleForm editable="true" layout="ResponsiveGridLayout" labelSpanXL="3" labelSpanL="3" labelSpanM="3" labelSpanS="12" columnsXL="2" columnsL="2" columnsM="1">
                                <core:Title text="Credit Profile"/>
                                
                                <Label text="Risk Class"/>
                                <Select selectedKey="{wizardData>/RiskClass}" items="{path: '/VH_RiskClass', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" enabled="false">
                                    <core:Item key="{code}" text="{code} - {name}"/>
                                </Select>
                                
                                <Label text="Check Rule"/>
                                <Select selectedKey="{wizardData>/CheckRule}" items="{path: '/VH_CheckRule', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" enabled="false">
                                    <core:Item key="{code}" text="{code} - {name}"/>
                                </Select>
 
                                <Label text="Credit Group"/>
                                <Select selectedKey="{wizardData>/CreditGroup}" items="{path: '/VH_CreditGroup', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" enabled="false">
                                    <core:Item key="{code}" text="{code} - {name}"/>
                                </Select>
                            </form:SimpleForm>

                            <Table items="{wizardData>/CreditSegments}" noDataText="No Credit Segment data. Click '+' to add." class="sapUiMediumMarginTop">
                                <headerToolbar>
                                    <OverflowToolbar>
                                        <Title text="Credit Limit and Control"/>
                                        <ToolbarSpacer />
                                    </OverflowToolbar>
                                </headerToolbar>
                                <columns>
                                    <Column><Text text="Credit Segment"/></Column>
                                    <Column><Text text="Rules"/></Column>
                                    <Column><Text text="Limit Defined"/></Column>
                                    <Column><Text text="Limit"/></Column>
                                    <Column><Text text="Currency"/></Column>
                                    <Column><Text text="Valid To"/></Column>
                                    <Column hAlign="End" width="50px" visible="{= !${wizardData>/isReadOnly} }"></Column>
                                </columns>
                                <items>
                                    <ColumnListItem>
                                        <cells>
                                            <Select selectedKey="{wizardData>CreditSegment}" items="{path: '/VH_CreditSegment', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" enabled="false">
                                                <core:Item key="{code}" text="{code} - {name}"/>
                                            </Select>
                                            <Select selectedKey="{wizardData>CreditLimitRules}" items="{path: '/VH_CreditLimitRule', parameters: {$filter: 'isActive eq true'}}" forceSelection="false" enabled="false">
                                                <core:Item key="{code}" text="{code} - {name}"/>
                                            </Select>
                                            <RadioButtonGroup columns="2" selectedIndex="{= ${wizardData>LimitDefined} ? 0 : 1 }" editable="{= !${wizardData>/isReadOnly} }" select="onLimitDefinedChange">
                                                <RadioButton text="Defined" />
                                                <RadioButton text="Not Defined" />
                                            </RadioButtonGroup>
                                            <Input value="{wizardData>CreditLimit}" type="Number" placeholder="100" editable="false"/>
                                            <Input value="{wizardData>LimitCurrency}" editable="false" width="70px"/>
                                            <DatePicker value="{wizardData>ValidityDate}" valueFormat="yyyy-MM-dd" displayFormat="medium" editable="{= !${wizardData>/isReadOnly} }"/>
                                            <Button icon="sap-icon://delete" type="Reject" press="onRemoveCreditSegment" visible="{= !${wizardData>/isReadOnly} }"/>
                                        </cells>
                                    </ColumnListItem>
                                </items>
                            </Table>
                        </VBox>
                        <HBox justifyContent="SpaceBetween" class="sapUiMediumMarginTop">
                            <Button text="Previous" type="Transparent" press="onPrevStep" />
                            <HBox>
                                <Button text="Review &amp; Submit" type="Accept" press="onOpenReview" />
                            </HBox>
                        </HBox>
                    </WizardStep>

                </Wizard>
                </VBox>
            </VBox>
        </content>
    </Page>

    <Dialog id="reviewDialog"
            title="Review &amp; Submit"
            contentWidth="800px"
            draggable="true"
            resizable="true">
        <content>
            <VBox class="sapUiMediumMargin">
                <MessageStrip text="Review all data before submitting." type="Warning" showIcon="true" class="sapUiSmallMarginBottom"/>
                <form:SimpleForm layout="ColumnLayout" columnsL="2" columnsM="2" editable="false">
                    <core:Title text="General Data"/>
                    <Label text="Category"/><Text text="{wizardData>/BusinessPartnerCategory}"/>
                    <Label text="Type"/><Text text="{wizardData>/BPType}"/>
                    <Label text="Grouping"/><Text text="{wizardData>/Grouping}"/>
                    
                    <core:Title text="Address"/>
                    <Label text="Title" visible="{= ${wizardData>/BusinessPartnerCategory} !== '1' }"/><Text text="{wizardData>/Title}" visible="{= ${wizardData>/BusinessPartnerCategory} !== '1' }"/>
                    <Label text="Name" visible="{= ${wizardData>/BusinessPartnerCategory} !== 'Person' }"/><Text text="{wizardData>/Name}" visible="{= ${wizardData>/BusinessPartnerCategory} !== 'Person' }"/>
                    <Label text="Name" visible="{= ${wizardData>/BusinessPartnerCategory} === 'Person' }"/><Text text="{wizardData>/FirstName} {wizardData>/LastName}" visible="{= ${wizardData>/BusinessPartnerCategory} === 'Person' }"/>
                    <Label text="Search Terms"/><Text text="{wizardData>/SearchTerm1} / {wizardData>/SearchTerm2}"/>
                </form:SimpleForm>
                

                <Title text="Sales Areas" level="H4" class="sapUiSmallMarginTop"/>
                <Table items="{wizardData>/SalesAreas}">
                    <columns>
                        <Column><Text text="Sales Org"/></Column>
                        <Column><Text text="Dist Channel"/></Column>
                        <Column><Text text="Division"/></Column>
                        <Column><Text text="Payment Terms"/></Column>
                    </columns>
                    <items>
                        <ColumnListItem>
                            <cells>
                                <Text text="{wizardData>SalesOrganization}"/>
                                <Text text="{wizardData>DistributionChannel}"/>
                                <Text text="{wizardData>Division}"/>
                                <Text text="{wizardData>PaymentTerms}"/>
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>

                <Title text="Company Codes" level="H4" class="sapUiSmallMarginTop"/>
                <Table items="{wizardData>/CompanyCodes}">
                    <columns>
                        <Column><Text text="Company Code"/></Column>
                        <Column><Text text="Reconciliation Account"/></Column>
                    </columns>
                    <items>
                        <ColumnListItem>
                            <cells>
                                <Text text="{wizardData>CompanyCode}"/>
                                <Text text="{wizardData>ReconciliationAccount}"/>
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>

                <Title text="Credit Segments" level="H4" class="sapUiSmallMarginTop"/>
                <Table items="{wizardData>/CreditSegments}">
                    <columns>
                        <Column><Text text="Segment"/></Column>
                        <Column><Text text="Limit"/></Column>
                        <Column><Text text="Currency"/></Column>
                        <Column><Text text="Valid To"/></Column>
                    </columns>
                    <items>
                        <ColumnListItem>
                            <cells>
                                <Text text="{wizardData>CreditSegment}"/>
                                <Text text="{wizardData>CreditLimit}"/>
                                <Text text="{wizardData>LimitCurrency}"/>
                                <Text text="{wizardData>ValidityDate}"/>
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>
            </VBox>
        </content>
        <buttons>
            <Button text="Cancel" press="onCloseReview" type="Transparent"/>
            <Button text="Submit for Approval" press="onWizardCompleted" type="Emphasized" icon="sap-icon://paper-plane" visible="{= !${wizardData>/isReadOnly} }"/>
        </buttons>
    </Dialog>
</mvc:View>